global:
  scrape_interval: 1s
  evaluation_interval: 5s

scrape_configs:
  - job_name: daser
    static_configs:
      - targets:
          - host.docker.internal:2026

  - job_name: vllm
    static_configs:
      - targets:
          - host.docker.internal:8000
